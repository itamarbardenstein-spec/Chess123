<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Chess.Views.HomePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:m="clr-namespace:Chess.Models"
    xmlns:vm="clr-namespace:Chess.ViewModel"
    Title="HomePage"
    x:DataType="vm:HomePageVM">
    <VerticalStackLayout Spacing="20">
        <Button HorizontalOptions="End" BackgroundColor="Transparent">
            <Button.ImageSource>
                <FontImageSource Glyph="{StaticResource IconSettings}" Size="40" Color="Gray">
                    <FontImageSource.FontFamily>MaterialSymbols</FontImageSource.FontFamily>
                </FontImageSource>
            </Button.ImageSource>
        </Button>
        <Label
            FontSize="30"
            HorizontalOptions="Center"
            Text="{Binding UserName, StringFormat='Hello {0}!'}"
            TextColor="Black" />
        <Button Command="{Binding PlayCommand}"  Text="{x:Static m:Strings.Play}" />
        <Button Text="{x:Static m:Strings.Instructions}"/>
        
    </VerticalStackLayout>
</ContentPage>